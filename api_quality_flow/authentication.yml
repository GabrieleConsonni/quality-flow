info:
  name: authentication
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/mock/api-server/api-docs"
  headers:
    - name: akn-device-id
      value: TECH
  auth:
    type: basic
    username: admin
    password: admin

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
