info:
  name: opportunity query
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/mock/salesforce-mock/salesforce.com/services/data/v59.0/query"
  headers:
    - name: akn-device-id
      value: TECH
  auth:
    type: basic
    username: bruno
    password: password

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
